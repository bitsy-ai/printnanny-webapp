# automatically generated by the FlatBuffers compiler, do not modify

# namespace: telemetry_event

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class MonitoringFrame(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAsMonitoringFrame(cls, buf, offset):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MonitoringFrame()
        x.Init(buf, n + offset)
        return x

    # MonitoringFrame
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MonitoringFrame
    def Ts(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # MonitoringFrame
    def Image(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from telemetry_event.Image import Image
            obj = Image()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MonitoringFrame
    def BoundingBoxes(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from telemetry_event.BoundingBoxes import BoundingBoxes
            obj = BoundingBoxes()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def MonitoringFrameStart(builder): builder.StartObject(3)
def MonitoringFrameAddTs(builder, ts): builder.PrependUint32Slot(0, ts, 0)
def MonitoringFrameAddImage(builder, image): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(image), 0)
def MonitoringFrameAddBoundingBoxes(builder, boundingBoxes): builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(boundingBoxes), 0)
def MonitoringFrameEnd(builder): return builder.EndObject()

import telemetry_event.BoundingBoxes
import telemetry_event.Image
try:
    from typing import Optional
except:
    pass

class MonitoringFrameT(object):

    # MonitoringFrameT
    def __init__(self):
        self.ts = 0  # type: int
        self.image = None  # type: Optional[telemetry_event.Image.ImageT]
        self.boundingBoxes = None  # type: Optional[telemetry_event.BoundingBoxes.BoundingBoxesT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        monitoringFrame = MonitoringFrame()
        monitoringFrame.Init(buf, pos)
        return cls.InitFromObj(monitoringFrame)

    @classmethod
    def InitFromObj(cls, monitoringFrame):
        x = MonitoringFrameT()
        x._UnPack(monitoringFrame)
        return x

    # MonitoringFrameT
    def _UnPack(self, monitoringFrame):
        if monitoringFrame is None:
            return
        self.ts = monitoringFrame.Ts()
        if monitoringFrame.Image() is not None:
            self.image = telemetry_event.Image.ImageT.InitFromObj(monitoringFrame.Image())
        if monitoringFrame.BoundingBoxes() is not None:
            self.boundingBoxes = telemetry_event.BoundingBoxes.BoundingBoxesT.InitFromObj(monitoringFrame.BoundingBoxes())

    # MonitoringFrameT
    def Pack(self, builder):
        if self.image is not None:
            image = self.image.Pack(builder)
        if self.boundingBoxes is not None:
            boundingBoxes = self.boundingBoxes.Pack(builder)
        MonitoringFrameStart(builder)
        MonitoringFrameAddTs(builder, self.ts)
        if self.image is not None:
            MonitoringFrameAddImage(builder, image)
        if self.boundingBoxes is not None:
            MonitoringFrameAddBoundingBoxes(builder, boundingBoxes)
        monitoringFrame = MonitoringFrameEnd(builder)
        return monitoringFrame
